const catalog=()=>window.AC_BUYER_CATALOG||[];
const sellerItems=()=>{try{return JSON.parse(localStorage.getItem('ac-artisan-artworks')||'[]')}catch{return[]}};
const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const groups={woodcraft:['Woodcraft'],pottery:['Ceramics'],ceramic:['Ceramics'],ceramics:['Ceramics'],jewellery:['Embroidery & Textile'],jewelry:['Embroidery & Textile'],'textile art':['Embroidery & Textile'],embroidery:['Embroidery & Textile'],bags:['Bags','Embroidery & Textile'],bag:['Bags','Embroidery & Textile'],basket:['Baskets','Bamboo & Cane'],baskets:['Baskets','Bamboo & Cane'],painting:['Paintings'],paintings:['Paintings'],sculpture:['Metal Art','Woodcraft','Showpieces'],'home decor':['Metal Art','Woodcraft','Ceramics','Showpieces'],attar:['Attar'],perfume:['Attar'],bamboo:['Bamboo & Cane'],'pottery & ceramics':['Ceramics'],'embroidery & textiles':['Embroidery & Textile'],'attar / perfumes':['Attar']};
const GROUP_SPRITES={dat:'/catalog/attar.webp',dbg:'/catalog/bags.webp',dbs:'/catalog/baskets.webp',dsp:'/catalog/showpieces.webp'};
const OLD_INDEX={dat1:0,dat2:1,dat3:11,dat4:22,dat5:33,dat6:44,dat7:46,dat8:47,dat9:48,dat10:49,dbg1:2,dbg2:3,dbg3:4,dbg4:5,dbg5:6,dbg6:7,dbg7:8,dbg8:9,dbg9:10,dbg10:12,dbs1:13,dbs2:14,dbs3:15,dbs4:16,dbs5:17,dbs6:18,dbs7:19,dbs8:20,dbs9:21,dbs10:23,dsp1:24,dsp2:25,dsp3:26,dsp4:27,dsp5:28,dsp6:29,dsp7:30,dsp8:31,dsp9:32,dsp10:34,dpt1:35,dpt2:36,dpt3:37,dpt4:38,dpt5:39,dpt6:40,dpt7:41,dpt8:42,dpt9:43,dpt10:45};
for(let i=1;i<=50;i++)OLD_INDEX['x'+i]=49+i;
function imageStyle(item){const id=String(item?.id||''),m=id.match(/^(dat|dbg|dbs|dsp)(\d+)$/);if(m&&GROUP_SPRITES[m[1]]){const n=Math.max(0,Math.min(9,Number(m[2])-1)),x=(n%5)*25,y=n<5?0:100;return`background-image:url('${GROUP_SPRITES[m[1]]}');background-size:500% 200%;background-position:${x}% ${y}%;background-repeat:no-repeat`}const n=OLD_INDEX[id];if(n==null)return'';const col=n%10,row=Math.floor(n/10);return`background-image:url('/catalog-100-sprite.jpg');background-size:1000% 1000%;background-position:${(col/9)*100}% ${(row/9)*100}%;background-repeat:no-repeat`}
function numToken(s){s=String(s).toLowerCase().replace(/,/g,'');const n=parseFloat(s);return Number.isFinite(n)?n*(s.includes('k')?1000:1):0}
function fixedPrice(range){const vals=(String(range||'').match(/[0-9][0-9,.]*\s*k?/gi)||[]).map(numToken).filter(Boolean);if(!vals.length)return 0;const raw=vals.length>1?(vals[0]+vals[1])/2:vals[0];if(raw<500)return Math.round(raw/10)*10;if(raw<2000)return Math.round(raw/50)*50;return Math.round(raw/100)*100}
const money=n=>'₹'+Number(n||0).toLocaleString('en-IN');
function budgetRange(v){const s=String(v||'').replace(/,/g,'').toLowerCase();if(!s||/no fixed/.test(s))return[0,Infinity];const nums=(s.match(/[0-9]+(?:\.[0-9]+)?/g)||[]).map(Number);if(/less than|under/.test(s))return[0,nums[0]||0];if(/\+$/.test(s))return[nums[0]||0,Infinity];if(nums.length>=2)return[nums[0],nums[1]];return[0,Infinity]}
function getBuyer(){try{return JSON.parse(localStorage.getItem('ac-buyer-details')||'{}')}catch{return{}}}
function normalizeSeller(x){return{id:x.id||`seller-${Date.now()}`,name:x.name||x.title,type:x.type||x.craft||'Handmade',category:x.category||x.craft||'Handmade',price:x.price||'₹0',image:x.image||'',artisan:x.artisan||'Local artisan',location:x.location||'India',sellerEntered:true}}
function unique(items){const seen=new Set();return items.filter(x=>{const k=x.id||`${x.name}|${x.type}`;if(seen.has(k))return false;seen.add(k);return true})}
function chooseItems(){const seller=sellerItems().map(normalizeSeller),all=[...seller,...catalog()];const b=getBuyer(),pref=String(b.preference||b.interests?.[0]||'').trim(),q=pref.toLowerCase(),[min,max]=budgetRange(b.budget);const budgetOk=x=>{const p=fixedPrice(x.price);return p>=min&&p<=max};let exact=all.filter(x=>`${x.name} ${x.type} ${x.category}`.toLowerCase().includes(q));if(groups[q])exact=[...exact,...all.filter(x=>groups[q].some(g=>String(x.category).toLowerCase().includes(g.toLowerCase())))];if(!exact.length){const words=q.split(/\s+/).filter(x=>x.length>2);exact=all.filter(x=>words.some(w=>`${x.name} ${x.type} ${x.category}`.toLowerCase().includes(w)))}const combined=unique([...unique(exact).filter(budgetOk),...all.filter(budgetOk),...exact,...all]);combined.sort((a,b)=>(b.sellerEntered?1:0)-(a.sellerEntered?1:0));return{pref:pref||'handmade craft',items:combined.slice(0,18)}}
function reason(item,pref){return item.sellerEntered?`New seller listing matching your ${pref} choice.`:`Matches your ${pref} preference with a fixed catalog price.`}
function visual(item){if(item.sellerEntered&&item.image)return `<img src="${esc(item.image)}" alt="${esc(item.name)}" loading="lazy" decoding="async">`;return `<div class="ac-sprite-image" style="${imageStyle(item)}" role="img" aria-label="${esc(item.name)}"></div>`}
function checkout(item){document.querySelector('#ac-data-checkout')?.remove();const b=getBuyer(),price=money(fixedPrice(item.price)),m=document.createElement('div');m.id='ac-data-checkout';m.className='modal-backdrop';m.innerHTML=`<div class="login-modal"><button class="modal-close">×</button><span class="eyebrow">CHECKOUT</span><h2>${esc(item.name)}</h2><p><b>${price}</b> · fixed demo price</p><p><b>Deliver to</b><br>${esc(b.name||'Buyer')}<br>${esc(b.phone||'')}<br>${esc(b.address||'')}</p><label>Choose payment method<select id="ac-data-pay"><option>UPI</option><option>Card</option><option>Cash on Delivery</option></select></label><button class="modal-submit" id="ac-data-place">Place demo order →</button></div>`;document.body.appendChild(m);m.querySelector('.modal-close').onclick=()=>m.remove();m.onmousedown=e=>{if(e.target===m)m.remove()};m.querySelector('#ac-data-place').onclick=()=>{const order={id:Date.now(),title:item.name,craft:item.type,price,payment:m.querySelector('#ac-data-pay').value,buyer:b,status:'New'};let list=[];try{list=JSON.parse(localStorage.getItem('ac-demo-orders')||'[]')}catch{}list.push(order);localStorage.setItem('ac-demo-orders',JSON.stringify(list));m.querySelector('.login-modal').innerHTML=`<span class="eyebrow">ORDER CONFIRMED</span><h2>Order placed successfully!</h2><p>${esc(item.name)} · <b>${price}</b></p><p>This is a hackathon demo checkout. No real payment was charged.</p><button class="btn primary" id="ac-data-done">Continue shopping</button>`;m.querySelector('#ac-data-done').onclick=()=>m.remove()}}
function render(){if(localStorage.getItem('ac-user-role')!=='buyer')return;const grid=document.querySelector('.buyer-products');if(!grid)return;const{items,pref}=chooseItems();if(!items.length)return;const sig=pref+'|'+items.map(x=>x.id).join(',');if(grid.dataset.catalogSig===sig)return;grid.dataset.catalogSig=sig;grid.innerHTML=items.map((x,i)=>`<article class="ac-ai-data-card" data-product-id="${esc(x.id)}"><div class="ac-ai-data-art">${visual(x)}</div><div class="buyer-product-info"><small>${esc(x.type)} · ${esc(x.location||'India')}</small><b>${esc(x.name)}</b><strong>${money(fixedPrice(x.price))}</strong><small>${Math.max(72,98-i*2)}% match</small><p>${esc(reason(x,pref))}</p><div class="ac-fixed-tag">${x.sellerEntered?`New seller listing · ${esc(x.artisan)}`:'Fixed price · uploaded catalog'}</div><button class="btn primary" data-buy="${esc(x.id)}">Buy now →</button></div></article>`).join('');grid.querySelectorAll('[data-buy]').forEach(b=>b.onclick=()=>checkout(items.find(x=>x.id===b.dataset.buy)))}
const style=document.createElement('style');style.textContent=`.ac-ai-data-card{overflow:hidden}.ac-ai-data-art{height:230px;position:relative;background:#ead3bd;overflow:hidden}.ac-ai-data-art>img,.ac-sprite-image{width:100%;height:100%;object-fit:cover;display:block}.ac-fixed-tag{display:inline-flex;align-self:flex-start;padding:5px 8px;border-radius:999px;background:rgba(188,82,43,.09);color:var(--rust);font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.04em}.ac-ai-data-card .buyer-product-info{height:auto}.ac-ai-data-card .buyer-product-info p{min-height:44px}`;document.head.appendChild(style);window.addEventListener('storage',render);window.addEventListener('load',render,{once:true});setTimeout(render,250);document.addEventListener('click',e=>{if(e.target.closest('.dashboard-sidebar button,.ac-art-options button'))setTimeout(render,80)},true);
